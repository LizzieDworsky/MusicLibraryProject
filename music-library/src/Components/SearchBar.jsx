import React from "react";

const SearchBar = (props) => {
    return (
        <form action="submit">
            {" "}
            <input type="text" />{" "}
        </form>
    );
};

export default SearchBar;
